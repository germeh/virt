# Debian preseed for the first Virt warm-migration appliance node.
# This file automates a minimal Debian install and runs post-install.sh
# from /opt/virt-appliance after the target system is installed.

d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us
d-i time/zone string UTC
d-i clock-setup/utc boolean true
d-i clock-setup/ntp boolean true

d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string virt-node
d-i netcfg/get_domain string local

d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

d-i passwd/root-login boolean false
d-i passwd/user-fullname string Virt Admin
d-i passwd/username string virtadmin
d-i passwd/user-password password virtadmin
d-i passwd/user-password-again password virtadmin
d-i user-setup/allow-password-weak boolean true
d-i user-setup/encrypt-home boolean false

d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string openssh-server sudo curl ca-certificates git qemu-system-x86 libvirt-daemon-system libvirt-clients virtinst bridge-utils ovmf cloud-image-utils
d-i pkgsel/upgrade select none
popularity-contest popularity-contest/participate boolean false

d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i finish-install/reboot_in_progress note

d-i preseed/late_command string \
    mkdir -p /target/opt/virt-appliance; \
    cp /cdrom/virt/post-install.sh /target/opt/virt-appliance/post-install.sh; \
    chmod +x /target/opt/virt-appliance/post-install.sh; \
    sed -i 's/^#*PasswordAuthentication.*/PasswordAuthentication no/' /target/etc/ssh/sshd_config; \
    in-target chage -d 0 virtadmin; \
    in-target /opt/virt-appliance/post-install.sh
